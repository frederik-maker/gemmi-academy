/*
 * Drop this registerPlugin call into:
 *   android/app/src/main/java/co/bussler/gemmi/MainActivity.java
 *
 * (or wherever your Capacitor MainActivity is — the install script auto-detects)
 */

// at top:
import co.bussler.gemmi.GemmiTutorPlugin;

// inside onCreate, before super.onCreate(savedInstanceState):
registerPlugin(GemmiTutorPlugin.class);
